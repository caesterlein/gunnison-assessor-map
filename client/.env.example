# Vite Environment Variables
# Copy this file to .env and adjust values as needed

# Tipg API URL (OGC Features API backend)
# In development (npm run dev), this is used when window.location.port === "5173"
# In production, the app uses /api proxy via nginx
VITE_TIPG_URL=http://localhost:8000
